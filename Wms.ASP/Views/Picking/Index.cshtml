@model PickingViewModel
@{
    ViewData["Title"] = "Picking Operations";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h2 class="mb-0">
        <i class="bi bi-box-arrow-up me-2 text-primary"></i>
        Picking Operations
    </h2>
</div>

<div class="row justify-content-center">
    <div class="col-lg-8">
        <div class="card border-0 shadow">
            <div class="card-header bg-primary bg-opacity-10 border-bottom">
                <h5 class="card-title mb-0 text-primary">
                    <i class="bi bi-box-arrow-up me-2"></i>
                    Pick Items for Orders
                </h5>
            </div>
            <div class="card-body">
                <form asp-action="Pick" method="post">
                    <div class="row g-3">
                        <!-- Order Information -->
                        <div class="col-12">
                            <label for="OrderNumber" class="form-label">Order Number <span class="text-danger">*</span></label>
                            <div class="input-group">
                                <span class="input-group-text">
                                    <i class="bi bi-receipt"></i>
                                </span>
                                <input asp-for="OrderNumber" class="form-control" placeholder="Enter order number" required autofocus/>
                            </div>
                            <span asp-validation-for="OrderNumber" class="text-danger"></span>
                            <div class="form-text">Enter the order or reference number for this pick</div>
                        </div>

                        <!-- Item Information -->
                        <div class="col-md-6">
                            <label for="ItemSku" class="form-label">Item SKU <span class="text-danger">*</span></label>
                            <div class="input-group">
                                <span class="input-group-text">
                                    <i class="bi bi-upc-scan"></i>
                                </span>
                                <input asp-for="ItemSku" class="form-control" placeholder="Scan or enter SKU" required/>
                            </div>
                            <span asp-validation-for="ItemSku" class="text-danger"></span>
                        </div>

                        <div class="col-md-6">
                            <label for="Quantity" class="form-label">Quantity to Pick <span class="text-danger">*</span></label>
                            <div class="input-group">
                                <span class="input-group-text">
                                    <i class="bi bi-123"></i>
                                </span>
                                <input asp-for="Quantity" class="form-control" type="number" step="0.01" min="0.01" required/>
                            </div>
                            <span asp-validation-for="Quantity" class="text-danger"></span>
                        </div>

                        <div class="col-12">
                            <label for="LocationCode" class="form-label">Pick From Location <span class="text-danger">*</span></label>
                            <div class="input-group">
                                <span class="input-group-text">
                                    <i class="bi bi-geo-alt"></i>
                                </span>
                                <input asp-for="LocationCode" class="form-control" placeholder="Enter location code" required/>
                            </div>
                            <span asp-validation-for="LocationCode" class="text-danger"></span>
                        </div>

                        <!-- Optional Fields -->
                        <div class="col-md-6">
                            <label for="LotNumber" class="form-label">Lot Number</label>
                            <div class="input-group">
                                <span class="input-group-text">
                                    <i class="bi bi-tag"></i>
                                </span>
                                <input asp-for="LotNumber" class="form-control" placeholder="Enter lot number (if required)"/>
                            </div>
                            <span asp-validation-for="LotNumber" class="text-danger"></span>
                        </div>

                        <div class="col-md-6">
                            <label for="SerialNumber" class="form-label">Serial Number</label>
                            <div class="input-group">
                                <span class="input-group-text">
                                    <i class="bi bi-qr-code"></i>
                                </span>
                                <input asp-for="SerialNumber" class="form-control" placeholder="Enter serial number (if required)"/>
                            </div>
                            <span asp-validation-for="SerialNumber" class="text-danger"></span>
                        </div>

                        <div class="col-12">
                            <label for="Notes" class="form-label">Notes</label>
                            <textarea asp-for="Notes" class="form-control" rows="2" placeholder="Enter any notes about this pick operation (optional)"></textarea>
                            <span asp-validation-for="Notes" class="text-danger"></span>
                        </div>

                        <!-- Action Buttons -->
                        <div class="col-12 pt-3">
                            <div class="d-flex gap-2 justify-content-end">
                                <button type="button" class="btn btn-outline-secondary" onclick="clearForm()">
                                    <i class="bi bi-arrow-clockwise me-1"></i>
                                    Clear
                                </button>
                                <button type="submit" class="btn btn-primary">
                                    <i class="bi bi-check-circle me-1"></i>
                                    Complete Pick
                                </button>
                            </div>
                        </div>
                    </div>
                </form>
            </div>
        </div>

        <!-- Quick Actions -->
        <div class="card border-0 shadow-sm mt-4">
            <div class="card-header bg-light border-bottom">
                <h6 class="card-title mb-0">
                    <i class="bi bi-lightning me-2"></i>
                    Quick Actions
                </h6>
            </div>
            <div class="card-body">
                <div class="row g-2">
                    <div class="col-md-6">
                        <a asp-controller="Receiving" asp-action="Receive" class="btn btn-outline-success w-100">
                            <i class="bi bi-box-arrow-in-down me-2"></i>
                            Go to Receiving
                        </a>
                    </div>
                    <div class="col-md-6">
                        <a asp-controller="Inventory" asp-action="Index" class="btn btn-outline-info w-100">
                            <i class="bi bi-stack me-2"></i>
                            Check Inventory
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
    <script>
        function clearForm() {
            $('form')[0].reset();
            $('#OrderNumber').focus();
        }

        $(document).ready(function() {
            // Focus on order number input after successful submission
            @if (TempData["SuccessMessage"] != null)
            {
                <text>
                setTimeout(function() {
                    $('#OrderNumber').focus();
                }, 100);
                </text>
            }

            // Enter key navigation
            $('input, textarea').on('keypress', function(e) {
                if (e.which === 13 && !$(this).is('textarea')) {
                    e.preventDefault();
                    var inputs = $('input[type!="hidden"]:visible, textarea:visible');
                    var nextInput = inputs.eq(inputs.index(this) + 1);
                    if (nextInput.length) {
                        nextInput.focus();
                    } else {
                        $('button[type="submit"]').focus();
                    }
                }
            });
        });
    </script>
}